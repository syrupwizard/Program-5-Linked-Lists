#include "list.h"


int main()
{
	node *head = nullptr;
	event an_event;
	
	runMenu(an_event, head);
//	addEvent(events);
//	displayEvent(events);	

//	insert(events, head);

//	displayList(head);

//	deleteList(head);
	return 0;
}
